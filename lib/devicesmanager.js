/**
 * Copyright reelyActive 2015-2024
 * We believe in an open Internet of Things
 */


const MIDI_MAPS = require('./utils/midimaps');
const CHANNEL_MAPS = require('./utils/channelmaps');


/**
 * DevicesManager Class
 * Maintains the state of devices and manages their events.
 */
class DevicesManager {

  /**
   * DevicesManager constructor
   * @param {Object} options The configuration options.
   * @constructor
   */
  constructor(options) {
    let self = this;
    options = options || {};
  }

}


module.exports = DevicesManager;
